const { x, sum } = require('./sum');
const { mul } = require('./mul');

module.exports = {
    x, sum, mul
} 

/** Calculate folder is a module 
 * index.js is importing sum and mul 
 * required functions are exported
*/